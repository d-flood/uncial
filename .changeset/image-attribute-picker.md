---
'uncial': minor
---

The `input: 'image'` field offers **Choose existing** when the `ImageSource` has `browse`: a modal picker with a lazily loaded thumbnail grid of the source's images. Images uploaded earlier in the session show their `blob:` preview. A browse-only source gets Choose existing and no Upload.
