---
'uncial': minor
---

Add a built-in `input: 'image'` attribute field with Upload and Clear, driven by an `ImageSource` passed to `Editor` (or the `imageSource` property of `uncial-editor`). A just-uploaded image renders on the canvas from a local `blob:` preview while the stored attribute keeps the source's returned value. Image fields work inside `list` items too.
