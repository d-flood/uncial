---
'uncial-cms': minor
---

`cmsImageSource` now browses: Choose existing lists the image files directly under `mediaDir` as served URLs, on GitHub and Local-only sites. Pass `list` to replace the listing and `thumbnail` to replace the tile URL, e.g. for a rendition pipeline.
