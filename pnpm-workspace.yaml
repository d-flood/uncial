packages:
  - 'packages/*'

allowBuilds:
  esbuild: true
  # astro, for the uncial-cms Astro fixture, which renders no images.
  sharp: false
  # uncial-cms-auth is a Cloudflare Worker; wrangler pulls workerd in.
  workerd: true
